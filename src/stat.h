// Common definitions and structures

#include "main.h"

extern void norm_cons(struct connection *); //divide connections, is they spread on few days
extern struct connection *mkstat(struct connection *, struct flags *); //make prestatistics
